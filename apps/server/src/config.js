export const PORT = Number(process.env.PORT || 4000)
export const ROOM_TTL_MS = Number(process.env.ROOM_TTL_MS || 60 * 60 * 1000)
export const PARTICIPANT_TTL_MS = Number(process.env.PARTICIPANT_TTL_MS || 60 * 1000)
export const SWEEP_INTERVAL_MS = Number(process.env.SWEEP_INTERVAL_MS || 10 * 1000)
